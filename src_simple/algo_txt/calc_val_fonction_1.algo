calc_val_fonction_1  -  11.03.2014

******************************************
calcule des valeurs de la fonction f(x) = x^2 pour x dans [-5 ; 5]
******************************************

VARIABLES
  x EST_DU_TYPE NOMBRE
DEBUT_ALGORITHME
  //boucle sur les valeurs de x
  POUR x ALLANT_DE -5 A 5
    DEBUT_POUR
    AFFICHERCALCUL x * x
    FIN_POUR
FIN_ALGORITHME
